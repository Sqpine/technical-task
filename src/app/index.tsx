import { withProviders } from "./providers";
import "./index.css";
import { Box } from "@mui/material";

const App = () => {
  return (
    <Box height="100vh">
    </Box>
  );
};

export default withProviders(App);